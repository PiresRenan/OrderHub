apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: orderhub-scale

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: deployment-scale-patch.yaml

images:
  - name: orderhub
    newName: orderhub
    newTag: oh-006