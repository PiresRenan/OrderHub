apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: orderhub-local

resources:
  - ../../base
  - namespace.yaml

images:
  - name: orderhub
    newName: orderhub
    newTag: 1.0.0