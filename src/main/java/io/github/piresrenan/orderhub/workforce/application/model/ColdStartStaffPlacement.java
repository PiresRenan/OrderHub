package io.github.piresrenan.orderhub.workforce.application.model;

import java.util.UUID;

/** Owner-local references prepared by the explicit first-Staff ceremony. */
public record ColdStartStaffPlacement(UUID departmentId, UUID positionId) {}
